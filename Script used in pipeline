properties([gitLabConnection(gitLabConnection: '', jobCredentialId: ''), parameters([choice(choices: 'main\nfeature-2\nfeature-3', description: 'select a branch to build now', name: 'branch')])])

node{
    stage('Scm Checkout'){
        echo "pulling changes from the branch ${params.branch}"
        git credentialsId: 'ur credentilas', url: 'your git URL', branch: "${params.branch}"
    }

}



POC5 ki use chesina repo ney same dheeniki use chestham, veyreydhi avasaram leydhu.
1st line okattey important. adhi kooda just syntax ye manam generate chesindhi. same adhey ah 1st line lo pettam.

ah syntax yela vachindho inkoka file add cheysi andhulo petta. choodandi.
